<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- FORM -->
    <record id="view_partner_form_activity" model="ir.ui.view">
        <field name="name">res.partner.form.activity</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='street']" position="before">
                <field name="activity_followup_count" readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- LISTA (SAFE MODE ODOO 19) -->
    <record id="view_partner_tree_activity" model="ir.ui.view">
        <field name="name">res.partner.tree.activity</field>
        <field name="model">res.partner</field>

        <!-- CLAVE: no dependas de base/contacts -->
        <field name="inherit_id" ref="base.view_res_partner_filter"/>

        <field name="arch" type="xml">

            <!-- INSERTA COLUMNA SIN DEPENDER DE STRUCTURE TREE -->
            <xpath expr="//field[@name='email']" position="after">
                <field name="activity_followup_count"/>
            </xpath>

        </field>
    </record>

</odoo>